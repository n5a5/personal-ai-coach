export default function GameifyPage() {
  return (
    <main style={{maxWidth:800,margin:'0 auto',padding:'40px 20px',fontFamily:'Arial'}}>
      <h1>Gameify</h1>
      <p>Earn points for actions that improve your life. Spend them deliberately on discretionary rewards.</p>
      <p>The interactive Gameify experience currently lives on the main dashboard; this route is reserved for the full history, rules, and rewards ledger.</p>
    </main>
  );
}

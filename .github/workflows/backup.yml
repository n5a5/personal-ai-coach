name: Supabase backup placeholder

# DISABLED: this repository is public, so database dumps must not be committed here.
# Configure a private backup destination before enabling automated data exports.

on:
  workflow_dispatch:

jobs:
  backup-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Database backup is intentionally disabled because this repository is public."
